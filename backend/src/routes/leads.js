import {Router} from 'express';import {z} from 'zod';import rateLimit from 'express-rate-limit';import {asyncHandler} from '../utils/asyncHandler.js';import {validate} from '../middleware/validate.js';import {authenticate,requireAdmin} from '../middleware/auth.js';import * as c from '../controllers/leadController.js';const router=Router();const schema=z.object({name:z.string().min(2).max(80).optional().default(''),email:z.string().email().optional().default(''),phone:z.string().min(8).max(20),company:z.string().max(120).optional().default(''),business:z.string().max(120).optional().default(''),location:z.string().max(120).optional().default(''),category:z.string().max(80).optional().default(''),need:z.string().max(80).optional().default(''),experience:z.string().max(40).optional().default(''),message:z.string().max(1000).optional().default(''),type:z.enum(['demo','callback'])});const leadRateLimit=rateLimit({windowMs:15*60*1000,max:10,standardHeaders:'draft-8',legacyHeaders:false,message:{success:false,message:'Too many requests, please try again later'}});router.post('/',leadRateLimit,validate(schema),asyncHandler(c.createLead));router.get('/',authenticate,requireAdmin,asyncHandler(c.listLeads));export default router
