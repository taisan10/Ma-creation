# Sirf static serve, /api Caddy handle karega (single proxy layer, clean):

server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    location / {
        try_files $uri /index.html;
    }
}