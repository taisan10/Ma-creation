import { serviceFallback, trainingFallback } from '../data'
import PlanCard from './PlanCard'
function normalize(list,fallback){return list.length?list:fallback.map(x=>({_id:null,name:x[0],price:x[1],billing:'one-time',features:Array.isArray(x[2])?x[2]:[x[2]]}))}
export function ServicePackagesSection({plans=[],cms={}}){const items=normalize(plans,serviceFallback);return <section data-font-section="plans.packages" id="packages" className="bg-paper2 section"><div className="wrap"><span className="eyebrow">{cms.serviceEyebrow||'GeM Service Packages'}</span><h2 className="h2 mt-3">{cms.serviceTitle||'Managed services'}</h2><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5 mt-10">{items.map(plan=><PlanCard key={plan._id||plan.name} plan={plan}/>)}</div></div></section>}
export function TrainingPackagesSection({plans=[],cms={}}){const items=normalize(plans,trainingFallback);return <section data-font-section="plans.training" id="training" className="section"><div className="wrap"><span className="eyebrow">{cms.trainingEyebrow||'GeM Training Programs'}</span><h2 className="h2 mt-3">{cms.trainingTitle||'Learn the portal, then run it confidently.'}</h2><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5 mt-10">{items.map(plan=><PlanCard key={plan._id||plan.name} plan={plan}/>)}</div></div></section>}
