import { retainerFeatures } from '../data'
function Cell({value}){return value===true?<span className="text-teal font-bold">✓</span>:value===false?<span className="text-ink/40">✕</span>:<span>{value}</span>}
export default function RetainerSection({cms={}}){return <section data-font-section="plans.retainer" id="retainer" className="section"><div className="wrap"><span className="eyebrow">{cms.retainerEyebrow||'Flexible Retainer Plans'}</span><h2 className="h2 mt-3">{cms.retainerTitle||'Better clarity with a side-by-side comparison'}</h2><div className="table-wrap overflow-x-auto mt-10"><table className="w-full min-w-[900px] border-collapse"><thead><tr><th className="th-cell">Features</th><th className="th-cell">6 Months · ₹12.5 Lakhs Order Cover</th><th className="th-cell">12 Months · ₹33.33 Lakhs Order Cover</th><th className="th-cell">24 Months · ₹80 Lakhs Order Cover</th><th className="th-cell">OEM · 36 Months · ₹2.0 Crores Order Cover</th></tr></thead><tbody>{retainerFeatures.map(row=><tr key={row[0]}><td className="td-cell font-semibold">{row[0]}</td>{row.slice(1).map((value,i)=><td className="td-cell" key={i}><Cell value={value}/></td>)}</tr>)}</tbody></table></div><p className="mt-4 text-xs text-muted">{cms.retainerNote||'The retainer reference specifies order-cover amounts and feature entitlements but does not provide a separate purchase price for the plans themselves.'}</p></div></section>}
