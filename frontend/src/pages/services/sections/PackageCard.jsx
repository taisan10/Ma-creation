export default function PackageCard({ plan }) { return <div className={`plan-card ${plan.featured?'featured':''}`}>{plan.featured&&<span className="absolute -top-3 right-5 bg-primary text-white font-mono text-[11px] px-2.5 py-1 rounded-full">Popular</span>}<h3 className="font-display text-[19px]">{plan.name}</h3><div className="font-mono text-[26px]">₹{Number(plan.price||0).toLocaleString('en-IN')}<small className="text-xs text-muted font-normal"> + GST</small></div><ul className="space-y-2 mt-3">{(plan.features||[]).map((it,i)=><li key={`${plan._id||plan.name}-${i}`} className="text-[13.8px] text-muted flex gap-2"><span className="text-secondary font-bold">✓</span><span>{it}</span></li>)}</ul></div> }
