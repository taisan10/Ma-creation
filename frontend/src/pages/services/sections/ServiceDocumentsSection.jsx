export default function ServiceDocumentsSection({ cms = {} }) { return <section data-font-section="services.documents" id="documents" className="section"><div className="wrap"><div className="max-w-[640px] mb-12"><span className="eyebrow">Documentation</span><h2 className="h2 mt-3">Documents required for GeM registration</h2></div><div className="grid grid-cols-1 md:grid-cols-2 gap-12">{[['For seller registration',cms.sellerDocuments||[]],['For buyer registration',cms.buyerDocuments||[]]].map(([title,docs])=><div key={title}><h3 className="font-display text-xl">{title}</h3>{docs.length?docs.map((d,i)=><div key={d} className="doc-item"><span className="doc-idx">{String(i+1).padStart(2,'0')}</span><span>{d}</span></div>):<p className="mt-4 text-sm text-muted">Add checklist items from Admin → Pages / CMS → Services.</p>}</div>)}</div></div></section> }
